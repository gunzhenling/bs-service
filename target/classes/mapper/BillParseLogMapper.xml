<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bs.payment.modules.trade.dao.BillParseLogMapper">

	<insert id="save" parameterType="BillParseLog" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO pay_center3_bill_parse_log (id, app_id, pay_type, bill_date, create_time, remark)
		VALUES (#{id}, #{appId}, #{payType}, #{billDate}, #{createTime}, #{remark})
	</insert>
	
	<select id="find" resultType="BillParseLog">
		select id, app_id, pay_type, bill_date, create_time, remark
		from pay_center3_bill_parse_log 
		where app_id = #{appId}
		and pay_type = #{payType}
		and bill_date = #{billDate}
	</select>
</mapper>